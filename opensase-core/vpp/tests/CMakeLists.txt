# Placeholder for VPP tests CMakeLists
cmake_minimum_required(VERSION 3.16)

# Python-based tests for VPP plugins
# VPP test framework uses Python for testing

# NOTE: VPP tests require VPP to be installed
# Run tests with: make test

message(STATUS "VPP tests configured")
